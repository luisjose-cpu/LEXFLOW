# LEVEL3_MEMORY_ENGINE

Legal Memory indexes tenant-scoped documents, timeline events and communications into searchable memory items with citations.

APIs:

- `POST /api/v1/intelligence/memory/index`
- `GET /api/v1/intelligence/memory/search`

Frontend: `/intelligence/memory`

Every indexed item keeps source metadata and must remain tenant isolated.
