# LEVEL3_LATAM_READY

LATAM Ready Engine introduces per-tenant country configuration for legal sources, currency, timezone, language and provider registry.

APIs:

- `GET /api/v1/latam/configs`
- `POST /api/v1/latam/configs/defaults`

Initial registry includes Peru, Colombia, Mexico and Chile as mock-ready configurations.
