import { AppShell } from "@/components/app-shell";
import { TelemetryCenter } from "@/components/level4-enterprise";

export default function TelemetryPage() {
  return (
    <AppShell>
      <TelemetryCenter />
    </AppShell>
  );
}
